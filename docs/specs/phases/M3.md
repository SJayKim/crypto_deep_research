# M3 — Fan-out + synthesizer + long-term READ

> Phase 4 of 6 · Epic index: [`../M0-M5-vertical-slice-epic.md`](../M0-M5-vertical-slice-epic.md)
> Prev → [M2 — One worker over A2A](./M2.md) · Next → [M4 — Memory writes + round-trip](./M4.md)
> Largest milestone. Internal parallelism below.

## Inherits (from: [M2](./M2.md))

- **`workers/market/service.py`** — the A2A service template the other 3 workers copy.
- **`orchestrator/dispatch.py`** (single-worker form) — generalized into fan-out here.
- **`orchestrator/app.py`** (plan→dispatch→return) — `planner` + `synthesize` nodes
  inserted here.
- **`contracts/report.py`** (from M0) — `SynthesisReport` + `DimensionGap` (TENSION-C
  coverage fields); **`memory.py`** `LongTermMemory` protocol (read side used here).

**Preconditions:** M2 acceptance criteria all pass (one worker over A2A; orchestrator
gets a `WorkerArtifact`; malformed request → `JsonRpcError`).

## Scope

Add `orderbook-worker` as the 2nd worker, then extract `workers/base.py` (C6 rule of
three), then add `sentiment-worker` + `onchain-worker`. `asyncio.gather` fan-out over
A2A (P9, never `Send`). Synthesizer merges artifacts into a `SynthesisReport` with
per-dimension coverage (TENSION-C). Planner reads long-term memory to pick the worker set
(TENSION-B long-term READ). Partial-failure policy (A3): synthesize on ≥1 artifact, mark
the rest unavailable; per-worker 30s timeout.

## Acceptance criteria

1. All 4 workers dispatch in parallel via `asyncio.gather` over A2A; total latency ≈
   slowest worker, not the sum (proves parallel, not sequential).
2. **Flagship isolation test (A2):** feed a 1000-row OHLCV fixture; assert (a) the
   market artifact is bounded (≤5 key_points, no raw array) AND (b) orchestrator
   state holds **zero** raw OHLCV arrays at any point (stub LLM, T7b).
3. **Partial test (TENSION-C):** force 1 worker to fail; report has
   `status="partial"`, the failed dimension in `dimensions_unavailable` with a
   reason, and the CLI surfaces it.
4. **Zero-artifact test (A3):** all 4 fail → `status="failed"`, every dimension
   listed with a reason, CLI exits non-zero.
5. **Timeout test (A3):** a worker exceeding `WORKER_TIMEOUT_S` is marked
   unavailable, not blocking; the gather still returns.
6. Planner reads long-term `watchlist()` / `facts(symbol)` and the chosen worker set
   reflects it (long-term READ trigger, stub memory).
7. `orchestrator/` is untouched when adding workers 3 and 4 (data-driven registry).

## Internal parallelism (worktree lanes)

Once `workers/base.py` is extracted (mid-M3, per C6), the remaining worker dirs are
disjoint and can run as separate worktree lanes (DESIGN "Worktree parallelization"):

```
extract workers/base.py ─┬─> [orderbook] lane
                         ├─> [sentiment] lane
                         └─> [onchain]   lane   (+ memory as a 4th lane into M4)
```

Order constraint: `orderbook-worker` must exist before the extract (rule of three needs
two concrete workers); `sentiment`/`onchain` follow the extract.

## Files

`workers/{orderbook,sentiment,onchain}/{agent,service}.py`, `workers/base.py`,
`orchestrator/{planner,dispatch,synthesize}.py`, `tests/test_isolation.py`,
`tests/test_partial.py`, `tests/test_zero_artifact.py`, `tests/test_timeout.py`,
`tests/test_planner_longterm_read.py`.

## Effort

orderbook ~2h, base extract ~2h, sentiment+onchain ~3h, synthesizer ~2h, planner READ
~1h, the 5 tests ~4h. Largest milestone; split into worktree lanes if desired.

## Applies (locked decisions)

P9, A2 (flagship), A3, C6, TENSION-B, TENSION-C, T7b, T8.

## Hands off (to: [M4](./M4.md))

- **`orchestrator/app.py`** with run lifecycle — the plan→dispatch→synthesize graph now
  has clear run-start / run-end points. M4 plugs episodic read at run-start and
  episodic/long-term write at run-end into exactly those points.
- **`orchestrator/planner.py`** reading `LongTermMemory` — M3 runs it against **seeded
  fixtures**; M4 makes the store self-populating so a real prior run feeds the planner.
- **`contracts/memory.py`** protocols — M4 implements `working` / `episodic` / `longterm`
  behind them.

**Seam:** the long-term **READ** path exists and shapes orchestration. M4 closes the loop
by adding the **WRITE** path so the next run's plan reflects the previous run.
