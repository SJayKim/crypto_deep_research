# M5 — Live data + packaging

> Phase 6 of 6 (terminal) · Epic index: [`../M0-M5-vertical-slice-epic.md`](../M0-M5-vertical-slice-epic.md)
> Prev → [M4 — Memory writes + round-trip](./M4.md)

## Inherits (from: [M4](./M4.md))

- **A correct, complete in-process slice** — 4 dimensions, memory round-trip, partial/
  zero coverage, all green on loopback ASGI (T8).
- **DB topology (A4)** — per-worker checkpointer DBs + orchestrator-owned episodic/
  long-term DB; compose maps these without changing ownership.
- **`mcp_server/sources/{base,fixture}.py`** (from M1) — the `DataSource` interface M5
  adds a live implementation behind.

**Preconditions:** M4 acceptance criteria all pass (episodic round-trip; long-term WRITE
changes the next plan; DB topology verified). The slice is correct in-process before M5
pays for live data + containers.

## Scope

Add `CoinGeckoSource` behind the same MCP tool interface, swap one tool (`get_ohlcv`)
from fixture to live with **zero agent code changes**; the other 3 stay on fixtures.
`docker-compose` brings up orchestrator + 4 workers + MCP server + DB as the single
real-OS-process-boundary proof (T8). API key from `.env`, rate-limit handling on the
live source.

## Acceptance criteria

1. `docker-compose up` then `python -m crypto_deep_research "analyze BTC now"` returns a
   `SynthesisReport` using live CoinGecko for `get_ohlcv`, fixtures for the rest.
2. The fixture→live swap touched only `sources/coingecko.py` + env; no diff under
   `workers/` or `orchestrator/` (proves the MCP tool boundary held).
3. CoinGecko key loads from `.env`; grep finds no hardcoded key (CLAUDE.md).
4. A simulated 429 from CoinGecko is handled (backoff or surfaced as a dimension
   gap), not an unhandled crash. Live calls appear only in this milestone's
   eval-style check, never in the deterministic suite (CLAUDE.md: no live API in tests).
5. Logs show 6 distinct processes under compose (real process-boundary proof).

## Files

`mcp_server/sources/coingecko.py`, `docker-compose.yml`, per-service `Dockerfile`(s),
`tests/test_source_swap.py` (asserts no agent-code diff path).

## Effort

CoinGeckoSource + rate limit ~3h, docker-compose + Dockerfiles ~3h, swap test ~1h.

## Applies (locked decisions)

A4 (MCP stateless), MCP boundary, T8 (compose), CLAUDE.md (keys, no live API in tests).

## Hands off (to: epic Definition of Done)

Terminal phase. On completion the epic's [Definition of Done](../M0-M5-vertical-slice-epic.md#definition-of-done-epic)
is met: `analyze BTC now` returns a `SynthesisReport` under `docker-compose up`, A2A runs
as real OS processes, the live swap changed no agent code, and the MCP tool boundary held.
